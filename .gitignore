node_modules/
dist/
*.tgz
.DS_Store

# Local staging for contributions to other repos (a11y-llm-eval, etc.) — never publish.
external-contribs/

# Held back from the public release until the findings are verified and
# upstream issues are filed (see NEXT_STEPS Phase 3.1/3.2). Remove these two
# lines and commit once that's done to publish the data report + paper.
reports/
paper/
